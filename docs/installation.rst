============
Installation
============

At the command line::

    $ easy_install django-handy

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-handy
    $ pip install django-handy
